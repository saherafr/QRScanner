<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DroidDesign" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/bg_dark</item>
        <item name="colorSecondary">@color/bg_dark</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="Theme.DroidDesign" parent="Base.Theme.DroidDesign">
        <item name="android:itemTextAppearance">@style/MenuItemTextStyle</item>
    </style>

</resources>